@model IEnumerable<ABCD_Admin.Models.Function>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutPage.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.functionName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.functionName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.functionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.functionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.functionId })
        </td>
    </tr>
}

</table>
